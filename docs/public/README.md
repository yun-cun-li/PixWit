# Public 目录说明

此目录下的所有文件会在构建时自动复制到网站根目录。

## 📄 latest.json

下载页面的配置文件，包含最新版本信息和下载链接。

### 文件结构

```json
{
  "version": "版本号",
  "releaseDate": "发布日期",
  "releaseNotes": "更新说明",
  "downloads": {
    "portable_7z": {
      "name": "便携版名称",
      "description": "描述",
      "size": "文件大小",
      "github": "GitHub 下载链接",
      "gitee": "Gitee 下载链接",
      "baiduPan": "百度网盘链接"
    },
    "setup": {
      // 安装版配置，结构同上
    }
  }
}
```

### 如何更新

1. 编辑 `docs/public/latest.json` 文件
2. 修改版本号、下载链接等信息
3. 运行构建部署脚本：
   ```bash
   /root/update_pixwit.sh
   ```

### ⚠️ 注意事项

- **不要**直接修改 `/var/www/pixwit/latest.json`，它会在下次部署时被覆盖
- **不要**将此文件放在 `docs/` 目录下，必须放在 `docs/public/` 目录
- VitePress 会自动处理 `public/` 目录下的所有文件

## 添加其他静态文件

如需添加其他静态文件（如图片、PDF 等），也可以放在此目录下：

```
docs/public/
├── latest.json
├── images/
│   └── screenshot.png
└── files/
    └── manual.pdf
```

访问路径：
- `https://pixwit.cn/latest.json`
- `https://pixwit.cn/images/screenshot.png`
- `https://pixwit.cn/files/manual.pdf`

